# accounting — Lean4 axiomatic theorem-proving for portfolio evaluation

Cross-project rules: `../CLAUDE.md`. The **numerical axis** of the three-kernel Lean4 architecture (root CLAUDE.md § "Lean4 axes"; `data/charters/studying/specs/lean4-charter-2026-06-10/SPEC.md` § 4); `/dat` is its instance of invariant 3 (top-tier LLMs in parallel, never manual). Scope charter: `data/charters/accounting/numerical-axis/CHARTER.md` (domain-vs-mechanism carve in its § 3; the `axiomatize-trading-2026-05-30` family is scope-resident there).

The financial-domain parallel of `proving/`. Same three-layer split, different domain: instead of complaints under federal statutes, accounting/ proves portfolio-level judgments (the five frameworks below) over OHLCV bars + indicator series + GICS sector mappings.

## The split (mirror of proving/)

| Layer | Where | Reads | Writes | Tools |
|---|---|---|---|---|
| **Formal kernel** | `Accounting/<Framework>/*.lean` | only Lean | only Lean | Lean elaborator. No I/O, no LLM calls, no memsearch. |
| **Predicate functions** | `predicates/<framework>/*.md` | one portfolio JSON + bar/indicator evidence (via `scripts/data_view.py`) | a single `Bool` (plus evidence + uncertainty) | Claude Code sub-agent (`context: fork`); MAY use memsearch's `memory-recall` skill. |
| **Driver** | `scripts/extract_facts.py` | manifest + portfolio | `Accounting/<Framework>/Facts.lean` (axioms) + audit JSON | `claude -p` invocations; model resolved via the shared `code/lean_tools/model_floor.py` seat (top-tier default — Hard rule 7). |

The verifiable proof IS the Lean elaboration trace produced by `lake build`.

## Frameworks

Each framework lives under `Accounting/<Framework>/` (kernel) + `predicates/<framework>/` (specs). Adding a new framework is a parallel exercise — see "Adding a new framework" below.

| Framework | Kernel | Specs |
|---|---|---|
| **Trend** | `Accounting/Trend/` | `predicates/trend/` |
| **Momentum** | `Accounting/Momentum/` | `predicates/momentum/` |
| **OptionsRisk** | `Accounting/OptionsRisk/` | `predicates/options-risk/` |
| **Sector** | `Accounting/Sector/` | `predicates/sector/` |
| **Drawdown** | `Accounting/Drawdown/` | `predicates/drawdown/` |
| **Regime** | `Accounting/Regime/` | `predicates/regime/` |
| **ExitDiscipline** | `Accounting/ExitDiscipline/` | `predicates/exit-discipline/` |
| **Hedging** | `Accounting/Hedging/` | `predicates/hedging/` |
| **Debate** | `Accounting/Debate/` | — (specs pending, Phase 4) |
| **Factor** | `Accounting/Factor/` (own `lean_lib AccountingFactor`) | — (Tier-0: NO LLM predicates — facts lift from the fit lane via `scripts/factor_facts.py`, `source: fit_report`) |

Two distinct evaluating-side lockstep contracts: a predicate RENAME must update `evaluating/web/src/islands/QresevApp.tsx` (the hawk Lean trace is embedded verbatim there); a framework ADD sweeps `FRAMEWORK_META` (`evaluating/web/src/lib/frameworks.ts`) + `EXPECTED_FRAMEWORKS` (`evaluating/web/tests/e2e/frameworks.spec.ts`) — the roster reaches QresevApp as a prop.

**Chartered expansion 6→11** (adopted 2026-07-12): Regime, Breadth, MeanReversion, ExitDiscipline, Hedging + extensions to Trend/Momentum/Sector/Drawdown/Debate — `data/specs/accounting-charter-expansion-2026-07-12/` (landing record + P3-remainder roadmap + P6/K-C4 gate re-homed 2026-08-06 to the Frameworks rows above + `ns:accounting/2`; the spec's § 6 Phases ledger is no longer the record of truth). Standing TA pins (BETA benchmark-first, LEARNINGS seq 106; Donchian PRIOR-bar channel, L-110) are carried per-predicate in `predicates/README.md` — never restated here. **Emit-enablement deferred** (kernel-first-without-UI is the supported state, § 2.2): the report.py/loci + evaluating `FRAMEWORK_META` legs ride ONE sweep — next-steps item 2.

## Layout

Directory tree → `predicates/README.md` § "accounting/ directory layout" (beside the 11-step "Adding a new framework" procedure that reads it).

## Workflow

Four steps — author kernel → author specs → run the driver → `lake build` verify: full recipe (driver invocation block + per-example-proof verify) → `predicates/README.md` § "Adding a new framework"; read before any driver run or verify.

## Adding a new framework

Full 11-step procedure → `predicates/README.md` § "Adding a new framework" (the declared spec-format home): kernel scaffold, `Accounting.lean` wiring, per-axiom specs, `examples/<newframework>-sample/`, plus the README-roster + this-file-table + `FRAMEWORK_META` sweep.

## Hard rules — DO NOT violate

1. **Lean files MUST NOT do I/O.** No `IO`, no `IO.Process.run`, no FFI. The kernel is pure.
2. **Predicate specs MUST NOT cite or read `Accounting/<Framework>/*.lean`.** Their world is the portfolio JSON + bar evidence + spec rubric.
3. **The driver MUST NOT make financial judgments.** It only routes calls and serialises results. Add fields to the JSON schema (or a new `framework` block field) rather than logic to the driver.
4. **`Facts.lean` is generated. Never hand-edit.** All `Accounting/<Framework>/Facts.lean` are gitignored.
5. **Defined-risk only — non-negotiable.** Any options leg outside the SIX root-chartered strategies (root CLAUDE.md § Defined-risk options) is REFUSED at the predicate layer. The kernel encodes the same restriction at the type level via the closed `Accounting.Strategy` enum. Both must remain in force. **Composition carve (ruled 2026-08-01 by the sole FINANCIALLY grantor, ns:evaluating/16; owner `evaluating/CLAUDE.md` § 4):** a multi-leg structure named outside the six neither widens the allow-list nor extends `Strategy` **iff** every leg is independently allow-listed **and** every short leg is covered by a position the same book holds. Worked cases (collar admitted, uncovered synthetic short refused) live at the owner.
6. **Bar shape canonical.** Any code that reads parquet uses the `{ts, o, h, l, c, v, adj_c}` columns; vendor names (Alpaca's `t`, etc.) never leak past `scripts/data_view.py`.
7. **Axiomatize with the top-tier model only.** Every LLM-backed extraction that produces a kept/committed result — the `extract_facts.py` driver AND the `dat-{cell,reconciler,cross,debate}` fan-out agents — runs on the latest top-tier model; haiku is forbidden for any retained artifact. Model resolves from the ONE shared seat `code/lean_tools/model_floor.py` (`QAGENTS_AXIOMATIZE_MODEL`; legacy `QAGENTS_PREDICATE_MODEL` honored second — **plain first-hit-wins, NOT "raise-only"**, so the legacy var can LOWER the tier; G1 still refuses any sub-top-tier resolve at exit 3. Root § Model policy). The anti-poison guard (`_protocol_violation`) rejects + retries number-less evidence and raises if persistent; wave archives stamp `"model"`. Bake-off rationale + gate: memory `feedback_predicate_model_opus_not_haiku`.
8. **Blind-fanout confluence is the ONLY correctness oracle — it has SIX leak channels** (all three axes; normative home `data/charters/studying/specs/lean4-charter-2026-06-10/axiomatize-shared-2026-07-04/SPEC.md` § 4a): (a) *filesystem write* — all cell scratch under its own disjoint `<out>/.scratch/`, never the shared session scratchpad; (b) *orchestrator prompt* — name the METHOD, never the ANSWER; (c) *reads* — blindness is a READ property (root sweeps, `git status`/`diff`, sibling `pending/trading-cells/`, frozen `waves/<id>/`); (d) *memsearch recall* — BANNED for cells; (e) *shared NAMESPACE* — bounding the CHANNEL ("`#check`, don't `cat`") does NOT bound the BREADTH: a cell may `#check` only names its own brief supplies, and may never enumerate / roster / `ls` / glob-copy / `#print prefix` / env-walk by ANY mechanism (L-141/L-142); (f) *reconciler prior-ADJUDICATION surfaces* — canonical Lean is licensed, prior tier verdicts are not. An oracle bug is invisible in the output; the only remedy for a breached wave is a fresh blind **re-slice** — never re-scoring the agreement metric. Contracts: `.claude/agents/dat-{cell,reconciler,debate}.md`; per-channel detail + incident history: memory `feedback_blind_fanout_oracle_channels`; open work: ns-13/ns-14.
8a. **The shared auditor changes under you repeatedly, and no recorded verdict updates itself.** `code/lean_tools/transcript_audit.py` ships to all three axes automatically; the per-change roster and what is still open are owned by `code/CLAUDE.md` § lean_tools — never counted here, because the count is the thing that goes stale. **The OPERATIONAL half (the standing re-run-after-grader-change obligation, `--regrade` semantics, the axis-specific C2 subject facts, and the 2026-07-31 re-graded wave's rules) → `.claude/skills/dat-manual/SKILL.md` § Step 7** (moved verbatim, spread 2026-08-17 M-A1) — read it before any audit run, re-grade, or quoting of a recorded verdict. **The pre-2026-07-29 re-adjudication (ns-50, `69fd6e787`) is CLOSED — three dispositions, none re-openable; walk them in memory/git, never re-derive here.** One forward-looking clause survives: wave `fee9d201`'s violations were MECHANISM violations (wildcard-glob `Common/` olean staging = 8(e)/L-141 "by ANY mechanism") with disclosure measured ZERO — **the mechanism is what the next fence must catch, not the disclosure.** `review_channels` + `c5_rungs` ride each audit record and feed the 8b cert freeze directly. Answer-bearing docs a contract cites BY PATH must be declared via `--answer-key-glob` (C7) in the same change that creates them.
8b. **Wave-time blindness certification is MANDATORY (wired 2026-07-29).** `dat.sh --archive-wave --blindness-cert` freezes the audit verdict into `WAVE.json.blindness_certification` (closed-set `verdict`; ill-shaped dies; **absent ⇒ loud warning and NO key — an inferred certification is the defect**). The three standing rules (freeze BEFORE reacting to the auditor's exit code; search BOTH project slugs at the right transcript DEPTH and name them in the cert, L-162; a C5 `review` resting only on rungs `names`/`types` is contract-mandated while `roster` is substantive) and the full invocation are owned by `.claude/skills/dat-manual/SKILL.md` § Step 7. **Cert SHAPE moves, and a pre-2026-08-15 cert is REFUSED at HEAD** — the required blocks (`audit_params`, `rung_profile`, closed-set `review_channels`/`c5_rungs`) are enforced by `archive_wave.py`'s own refusal text, which fires only AFTER the Opus is spent: copy a LIVE cert, never an older one or this file's list. `--no-c7` rebuilds the blind spot — never take it. **The freeze binds harder here than on proving: accounting has NO cert store lane** (no `_cert_payload`, no `_store_cert_inproc`), so `WAVE.json` is the only record and `Ledger().blindness_certs("accounting")` returns a confident ZERO — see `ns:accounting/104`. Retention is owned by `data/charters/studying/specs/lean4-charter-2026-06-10/blindness-firewall-2026-07-25/SPEC.md` § Detection lane — never restated here. Witnesses: t_07 cert arms.
9. **A green build is not proof — `autoImplicit` must stay OFF.** `lakefile.toml` sets `autoImplicit = false, relaxedAutoImplicit = false` on all three `lean_lib`s (`Accounting`, `AccountingUniverse`, `examples`). With it ON, an unqualified `Common` type is silently bound as a **phantom implicit type variable**: the module builds green while its axioms range over an invented type — and a **bridge between two phantom-typed composites discharges vacuously**, so any agreement tier resting on it measured *nothing*. Never remove the option to make a file pass. History: memory `feedback_autoimplicit_phantom_types`. **Second vacuity mechanism, and NO option prevents this one (2026-08-16, seq 260): a bridge REFUTATION lemma taking both the positive and the negative composite as hypotheses** discharges by `absurd` on its own inconsistent premises — build green, `score_bridge` CONFLUENT, G7 clean, no `sorryAx`, judgment never touched. A refutation needs a REVERSE correspondence so the contradiction comes from the judgment's own field; without one, report the direction unavailable. **Detect on the AXIOM CLOSURE** — a real refutation names the reverse correspondence, a vacuous one names nothing.
9a. **The second way a green build proves nothing: an UNROOTED file is never elaborated, and a ROSTER-scoped gate cannot reach it.** `sorry` is a WARNING at rc 0, so both holes leave `lake build` "completed successfully". Closed 2026-08-06 (ns-62): `lakefile.toml` roots the `Universe/S*/{Volatility,Volume}/Theorems.lean` leaves the Bridge closure skips, and `dat.sh`'s G7 defaults to `--scan-dir Accounting` (147 theorems, ~0.4s) over one module (10); `DAT_ATTEST_MODULE` forces the narrow scope and the wave token records which. t_33 proof-of-fires the width: a `sorry` in an unimported module is rejected by the tree arm and **passes the roster arm**. Memory `feedback_unrooted_lean_file_is_never_elaborated`.

## Injected-context leak channel — THIS FILE reaches every blind cell

**A subproject `CLAUDE.md` is delivered to every subagent in its SYSTEM PROMPT. Every blindness channel grades a READ (C1–C5, C7); an injection is not a read — so answer-shaped prose here reaches every blind `/dat` cell with certainty and NO instrument on any axis can see it.** The system prompt is also absent from the subagent transcript, so a transcript-based check cannot find it and **its silence reads exactly like absence**: on the proving wave that found this (L-278, 2026-08-19), all six cells graded `review_mandated` — no re-slice owed — while every one held the answer key.

**So the golden judgment names, per-framework predicate rosters, golden example ids, tier-ladder thresholds and promoted-slice roster are NOT here** (split 2026-08-20, `ns:accounting/127`; proving + studying split the same week). They live in the golden roster beside the predicate specs, **which this file deliberately does not name by path** — naming the answer key in the injected file tells every cell where the answers are. Orchestrator-facing docs carry the pointer (`predicates/README.md`, the `/dat-manual` SKILL); the roster sits on the C7 `--answer-key-glob` set, so a contract citing it blocks the spawn and a cell reading it grades BREACH. **Keep this file free of identifiers** — the cure EMPTIES the channel, it does not GRADE it; no call site exists on any axis, so a later edit can refill it silently.

**AND THE INJECTED BLOCK IS SNAPSHOTTED AT SESSION START — CURING THIS FILE DOES NOT CURE THE RUNNING SESSION** (proving, seq 279: a subagent spawned minutes after the strip still quoted the identifiers while `grep` returned 0 on disk). **(a) Never slice a wave in a session that started before the cure** — those cells carry the stale snapshot and the wave is void as if nothing was fixed; open a NEW session. **(b) The cure is unverifiable from inside the session that applied it** — an in-session probe measures the SNAPSHOT, is right about it and wrong about the tree. Verify on disk with `grep`; confirm the context half only from a fresh session.

## Cross-project boundaries

- **OHLCV bars** — read via `scripts/data_view.py` from the shared `financial/parquet/ohlcv-equities/<SYM>.parquet` hub. Do NOT import `analyzing/`'s TypeScript DuckDB views or `trading/`'s `shared.lib` Python directly.
- **Indicator math** — `analyzing/scripts/ta_reference.py` (Python TA-Lib) is the ground truth. Predicate sub-agents MAY shell out to it as a Python escape hatch (per qagents/CLAUDE.md "Language split"). TA outputs land at `financial/parquet/ta-reference/<SYM>.parquet`.
- **GICS sectors** — `financial/parquet/gics-symbols.parquet` (read via DuckDB or via `python -m shared.lib.gics lookup` as a shell-out).
- **Downstream consumer** — `evaluating/web` (Qresev) calls `accounting/scripts/extract_facts.py` as a subprocess via `evaluating/server`; never imports `Accounting/<Framework>/*.lean` directly.
- **Financial sign-off** — `accounting/` **advises** the FINANCIALLY-CLEARED gate (framework/kernel verdicts) but is **not** the grantor; `evaluating/` is the sole grantor (`evaluating/CLAUDE.md` § 4).

## When a predicate sub-agent uses memsearch

A predicate sub-agent MAY invoke `memsearch:memory-recall` (`context: fork`) for prior thresholds / refusals / sector-cap calls; prior decisions live in `.memsearch/memory/YYYY-MM-DD.md`. Scoping + patch detail: root § memsearch. Blind dat cells are BANNED from recall — hard rule 8(d).

## Toolchain notes

- **No Mathlib dependency in the canonical build** (keeps `lake build` ~2s end-to-end). **Gated exception — R2.7, deferred, not yet triggered:** the per-axis Float-native numeric-lift pilot (axiomatize-trading § 15.3), a *gated* `require Mathlib` for real-valued order lemmas, fired only by a numeric-lift wave the integer-bps path cannot serve; the Mathlib-free integer-bps lift stands until then. **Dev prototype EXISTS: `accounting/dev-mathlib/`** — a separate, **gitignored, canonical-only** Lake package requiring Mathlib v4.30.0; Mathlib never enters git; lives at canonical (relocated out of worktrees at close) and recreates per `dev-mathlib/README.md`. **This bullet is THE authoritative tracked pointer to that package**; residual spec-side copies repoint here at their R-21 digestion.
- **Standing operator rulings** (extracted 2026-08-06 at the `NEXT_STEPS.md` retirement): **per-sector veto stays OUT** — `max_drawdown_leq 0.15` is a UNIVERSAL defined-risk bound; only the orthogonal concentration cap is per-sector parametrised (CapPolicy; operator-ruled 2026-06-16), and the S60/S35 drawdown-veto refusals are correct outcomes. **No sixth golden framework** — 5 covers the QresevApp v0 surface; the universe program's `Accounting/Debate/` is the only sanctioned 6th.
- **Driver smoke-test side effect:** `extract_facts.py` against an existing `examples/<id>/` overwrites all five artefacts in place — `git checkout -- examples/<id>/` before commit, or pass `--sandbox-out <dir>`.
- Rest (toolchain pin, authoring gotchas, `data_view.py` pandas-only, driver flag roster, `--stub` faithfulness, D8 timeouts) → memory `project_accounting_subproject__infra` § "Toolchain + driver CLI"; read before a toolchain bump or hand driver run.

## Results propagation — `scripts/{lean_parse,lake_trace,report}.py`

Mirror of proving/'s propagation pipeline (adapted for multi-judgment): each `extract_facts.py` run emits `report.json` + `graph.json` + `loci.json` under `examples/<id>/`. **Diff-check obligation against `proving/`:** `lake_trace.py` + `lean_parse.py` carry NO lockstep obligation — both are thin shims over the canonical shared `code/lean_tools/`, so patch the shared module. `report.py` shares internals with proving's — diff-check before patching either side. Deliberate accounting-side divergences (lowercase axiom names, dotted conclusion heads, multi-judgment `LociRoster` shape) are not drift — preserve them; the lean_parse ones now ride the shim's explicit axis parameters. Full module/emit-file detail + driver flags: memory `project_accounting_subproject__infra` § "Results propagation".

## Landed mechanisms & patterns (v1 notes)

Four landed mechanisms — **CapPolicy** (§ Toolchain notes standing rulings), **Real-call flip pattern**, **`scripts/build_examples.sh`**, **Hierarchical predicates** (`Accounting/Common/Tactic.lean` diff-locked with proving's; status of record `../studying/representation.md` § 13.4) — detail in memory `project_accounting_subproject__design` § "Landed mechanisms & patterns (v1 notes)". **Framework-level `Hier/` decomposition COMPLETE 2026-08-16 (8/8)**; the zero-read queue's successor ordering is DECLARED in the tool since `b3b20be4c` (seq 261). **A `Hier/` leaf spec MUST conform to its flat parent's pinned § Authority, and NO Lean gate reads a leaf spec** — the bridge asserts leaf and golden measure the same quantity, so a better quantity than the pin is as wrong as a worse one; only a real-call example on an already-discharged subject catches the drift (seq 262).

## Axiomatize-trading program (financial parallel of proving's axiomatize-uscode)

Spec: `data/charters/accounting/specs/axiomatize-trading-2026-05-30/SPEC.md`. Extends the five hand-built frameworks to a phased, **sector-by-sector** program over the S&P 500, produced redundantly by top-tier-model subagent teams along the ≥5 orthogonal **signal-family** axes (§ Axis set below), then reconciled. Cross-axis agreement proved in-kernel via `Bridge.lean` lemmas IS the correctness signal — the TA **confluence** heuristic. **The tier ladder's own thresholds are answer-shaped and live with the golden roster, never here** (§ Injected-context leak channel).

- **Slice unit:** the `(GICS sector, axis)` cell; sector code → namespace `Accounting.Universe.S<code>` (`S45` = Info Tech, the `T18` analog). GICS is the drill-down spine. Corpus = the existing `financial/parquet/` hub (`financial/gics/build.py --categorize` → `categorization.json`).
- **Golden reference:** the hand-built frameworks over the committed driver examples, scored via golden-bridges (NOT exact-string — naming is a free variable for a blind agent, per the uscode 2026-05-29 lesson). **Which frameworks, which examples, and their predicate rosters are the answer key** — they live with the golden roster, not here (§ Injected-context leak channel).
- **Debate framework:** `Accounting/Debate/` formalizes `trading/`'s bull-vs-bear gate. Its top-level judgment is a `structure` (no field omittable) requiring bull confluence + kernel directional accept + defined-risk cleanliness + the **negation of every bear disqualifier** — "veto, not a vote" as a type-level property. Field names and the judgment identifier are answer-shaped (§ Injected-context leak channel). Consumer wire is Phase 4, not yet landed: `debate-2026-05-30/SPEC.md` § 9.4.
- **Lane:** `/dat-manual` (Max-20x interactive) is the standing lane; the SDK batch lane (spec § 7.3) is deferred (root CLAUDE.md § Agent SDK lane owns status). The skill (`.claude/skills/dat-manual/`) runs Slice + Reconcile + Debate; mechanical phases in `scripts/dat.sh`, LLM fan-out over `.claude/agents/dat-{cell,reconciler,cross,debate}.md`. Zero-prompt enforced by `dat.sh --permit-fanout` vs `scripts/dat-helpers/required-allow-patterns.txt` (exit 61); cells write sandboxed `pending/trading-cells/`, promoted only post-reconcile. Bridge scorer = the **shared** `code/lean_tools/score_bridge.py` (`--root accounting --lexicon confluence`); **no `scripts/` file carries a byte-twin lockstep obligation** — `lake_trace.py` is a shim over shared code, `archive_wave.py` is diff-locked by convention (L-026). **Calibration integrity:** never hand-patch a blind cell/bridge to manufacture agreement — fix the ground-truth defect and re-run blind (memory `feedback_dau_gap_clear_blind_reslice_not_handpatch`).
- **Status (durable state).** The promoted-slice roster and the per-wave tier verdicts are adjudication figures and live with the golden roster, not here (§ Injected-context leak channel). Structurally: promoted work lands under the `AccountingUniverse` lean_lib behind explicit lakefile roots, its build is green, and the golden `Accounting` lib is unaffected. Veto refusals are correct outcomes. **Three-stage deterministic targeting is MANDATORY before any fan-out** — verbs, thresholds, guards and exit codes owned by `.claude/skills/dat-manual/SKILL.md` + spec § 6.5. Per hard-rule-4 the per-run Debate `Facts.lean` stays generated. Per-wave verdicts + the promoted set + targeting/tape detail: memory `project_accounting_subproject` + close summaries + spec § 6.5/§ 11/§ 13 + debate record `axiomatize-method-2026-06-09` (`data/debates/`, HELD lane).
- **Cadence plan (LIVE since W0 2026-07-11).** `…/dat-cadence-scaling-2026-07-10/SPEC.md` — Phase-4M analog with the domain INVERTED: accounting is demand-constrained (the tape gates admission), so it schedules detection+response, never capacity. G0 deterministic tape-refresh + `--frontier` gate (0 LLM; Tue/Fri + every session open; ingest **exit 3 = INV-H truncated-tape abort — never success, never retried past**, spec § 9.7; bare `dat.sh --frontier` IS the G0 verb) → breadth wave ≤2 trading days from a TIER-A-ELIGIBLE flip (ONE wave/tape) → depth increment otherwise. **Arm 2 is MECHANIZED at `dat.sh:_g0_route_on_zero`** — a zero-eligible resolve prints the routing + a TREE-derived work queue in the read's OWN output; observe-only, never a gate (a zero read is the gate working). Witness `t_65`; live queue + history: `ns:accounting/5`. Model/effort matrix + prompts: spec § 6 + PROMPTS.md. Never chase the numerator — the top-tier count is a tape outcome; the controllable metrics are detection/wave latency, depth coverage, gate greenness. **`accounting:frontier-staleness` (Tue+Fri 07:20, `scripts/frontier_staleness.py`) is an observe-only ALARM, never a G0 executor** — a cron G0 execution lane is ruled out by the worktree-branch requirement + the charter's detection-not-capacity rule, so session-open G0 stays mandatory. Its age arm is trading-day aware and every line prints the host; DARK until the operator-owed `install.sh --enable` on the seat host. Monthly review of this lane: `accounting/reviews/<date>-daily-routine-review.md`.
- **Wave archive (§ 12).** Blind fan-out sandboxes freeze to gitignored + S3-backed `accounting/waves/<wave-id>/` via `dat.sh --archive-wave` → `dat-helpers/archive_wave.py` (+ `WAVE.json`, tape `corpus_pin`); `/dat-manual` Step 7 archives BEFORE promote/hold (test `t_07_archive_wave.sh`), and that SKILL § owns the archived-contents roster, the gate order, and the LAN-primary backup posture. Port of proving's `/waves` — **diff-check both `archive_wave.py` in lockstep.** S3 class `trading-waves` (`serving/scripts/archive-artifact.sh`; serving owns the S3/lifecycle half). The verb also snapshots the tape behind the pin — one content-addressed `ohlcv-equities` copy per distinct `corpus_pin` at machine-local `accounting/tape-pins/<sha>/` (`--verify-pin`/`--restore-pin`, exit 5 fail-closed; t_56). **`pending/` is worktree-local** (NOT in `.worktree-links`, unlike `waves/`/`tape-pins/`), so anything left only in the sandbox dies at teardown — worth a pre-teardown glance for lane-novel artifacts the verb doesn't know yet.
- **Axis set (§ 6.2).** `DEFAULT_AXES` = 5 core + instrument-risk + liquidity (**7 per no-flag slice**); `CORE_AXES` stays the 5 the directional quorum references; corroborators never add to it (**the quorum's SIZE is an adjudication figure — § Injected-context leak channel**). Axes 8 Seasonality + 9 RiskQuantum **deferred**. Liquidity corroborator (failure = `bear_untradeable`): spec § 6.2 + `predicates/universe/_axes/liquidity.md`.
- **Gate + instrument mechanics → `accounting/gates.md`** (hermetic test lane · G8 archive-integrity · tape+wave fail-closed gates · `accounting:dat-state` sentinel — moved verbatim, spread 2026-08-17 M-A2). Orchestrator read: open it before diagnosing a `dat.sh` gate exit (4/17/20/21/22), arming or reading the `dat-state` sentinel, or extending the hermetic lane. A blind cell's brief never names it.
- **Progress visualization.** Domain graph (Half A, 526-node live |D|) × derivation graph (Half B, numerator) → quantify-progress spec § 5.1 (`.../lean4-charter-2026-06-10/quantify-progress-2026-06-14/SPEC.md`) + memory `project_quantify_progress_effort`; read before any `emits/lean_graph/` regen.

## Status emit (`data/status/accounting.json`)

`accounting/scripts/status_emit.py` (system `python3`, no venv) writes the StatusEmit slot; kit version is the producer's `KIT_VERSION` pin (swept in lockstep per root § Status hub). Surface roster + `productBrand: "Qresev"` + `latestProofGraphUrl` + two-lane writer: memory `[[project_accounting_subproject__infra]]` § Status emit surface. **Cron carrier = the whole-fleet `qagents:status-emit-all` ROUTINES entry, NOT a per-sub accounting slot** (the standalone 05:30/05:32 proving/accounting emits were RETIRED 2026-07-08). Grep `data/schedules/launchd/install.sh` ROUTINES for the live slot rather than trusting a time restated here; a per-sub time cited downstream outlived the routine by a month. The recent-runs table carries a `model` provenance pill (`_provenance`: stub→stubbed, reuse-facts→replay, absent→unknown-never-production); `dat.sh --lint-provenance` is the M-C 18 mirror lint over that emitted column + committed example `modelUsed`/uncertainty bands (`scripts/provenance_lint.py`, since 2026-08-15 a thin shim over the shared `code/lean_tools/provenance_lint_core.py` — patch the shared module, the shim carries only axis parameters; exit 3 non-exempt violation; proof-of-fire `scripts/test_provenance_lint.py`).

**Load-bearing invariants (stay inline):**

- **Two-file split (mirrors proving):** rollup `accounting/coverage.json` = committed Universe Bridge encodings (excludes golden) feeds the hub; numerator `emits/lean_graph/coverage.json` (live-|D| #print-axioms join, +golden) is the SEPARATE quantify-progress artifact — do not cross-wire; they differ by exactly the golden.
- **C6 coverage-parity:** `dat.sh --coverage-parity` (C6) enforces rollup-encoded == numerator-non-golden.
- **Coverage-emit traps → `accounting/gates.md` § Coverage emit** (moved verbatim, spread 2026-08-17 M-A4). Read before touching `derive_tier_counts`/`ACCOUNTING_COVERAGE_JSON`, editing tier_totals, or re-running `extract_facts.py --reuse-facts` against a committed example.
